---
ticket: TK-075
title: Backend — System Settings & FEFO Parameters API
epic: Configuración de Sistema
user_story: US-017
status: done
---

# 🎟️ Ticket Técnico: TK-075 — Backend System Settings API

> **Nota (`TK-110`, 2026-09-04):** este ticket estaba implementado desde hace varios tickets (`SystemSettings`, casos de uso, `GET/PUT /api/v1/settings`, `preparationWasteAlertPercent` sumado en `TK-105`) pero el `status` había quedado en `BACKLOG` — deuda de documentación, no de código. Corregido al detectar, de paso, que el Escenario 2 (umbral de alerta crítica FEFO) sí tenía un hueco real: `GetActiveRemanentesUseCase` no leía `criticalAlertHours` — ver `TK-110`.

## 🎯 Objetivo
Implementar el modelo `SystemSettings`, casos de uso (`GetSystemSettingsUseCase`, `UpdateSystemSettingsUseCase`), repositorio Prisma y endpoints HTTP `/api/v1/settings` para administrar la identidad del restaurante y los parámetros operativos FEFO.

---

## 🛠️ Tareas Técnicas
1. Definir `SystemSettings` domain entity y repositorio.
2. Implementar `settings.controller.ts` con validación Zod.
3. Conectar la lectura de `criticalAlertHours` y `currencySymbol` en las respuestas de la API.

---

## 🧪 Plan de Pruebas
- Pruebas unitarias para `UpdateSystemSettingsUseCase`.
- Pruebas de integración HTTP `GET /api/v1/settings` y `PUT /api/v1/settings`.
