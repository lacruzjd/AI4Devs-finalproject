---
document: technical_ticket
id: TK-094
related_story: N/A (Técnico — Deuda de Infraestructura)
points: 3
type: backend
priority: Should Have
status: done
inputs:
  - docs/audits/AUDIT-DEV-005-TK-092-quality-report.md (D-6 / FASE 3)
  - apps/backend/prisma/migrations/
  - apps/backend/prisma/schema.prisma
  - apps/backend/docker-entrypoint.sh
---

# 🎟️ TK-094: Paridad entre `prisma/migrations/` y `schema.prisma` (columnas `mustChangePin` / `email`)

> **Navegación del Framework SDD:**
> [📖 Índice de Tickets](../../indice_tickets.md) · [🛡️ AUDIT-DEV-005](../../../../audits/AUDIT-DEV-005-TK-092-quality-report.md)

---

## 📝 Descripción

`AUDIT-DEV-005` (revisión de TK-092), defecto **D-6**: el árbol `apps/backend/prisma/migrations/` está desincronizado de `schema.prisma`.

* `schema.prisma` declara dos columnas que ninguna migración añade:
  * `User.mustChangePin Boolean @default(true)` (Guard 36 / TK-070)
  * `SystemSettings.idleTimeoutMinutes Int @default(15)` (Guard 37 / TK-071)
* (`User.email` + `User_email_key` **sí** están cubiertos por `20260831234500_add_admin_pin_recovery_fields` — verificado con `prisma migrate diff`.)

El stack real **no lo nota** porque `apps/backend/docker-entrypoint.sh` aplica el esquema con `prisma db push --accept-data-loss`, que reconcilia el schema directamente e ignora `migrations/`. Pero:

1. `docs/04_governance_and_quality/scripts/check_seed_idempotency.sh` usa `prisma migrate deploy` (línea 65) → sobre una BD limpia falla con `P2022 ColumnNotFound` en `User.mustChangePin` → el gate de **FASE 3.4** del `04_dev_audit_workflow.md` queda ROJO para cualquier ticket que toque `seed.ts`, `schema.prisma` o migraciones (le pasó a TK-092, cuya reconciliación de huérfanos sí se verificó por inspección + stack vivo, pero no por el gate).
2. Cualquier consumidor que despliegue con `migrate deploy` (el propio comentario del `Dockerfile` lo menciona como objetivo) obtendría una BD rota.

---

## 🎯 Objetivo

`prisma migrate deploy` sobre una BD vacía produce exactamente el mismo esquema físico que `prisma db push` desde `schema.prisma` — sin drift. `check_seed_idempotency.sh` pasa en verde.

---

## 🔀 Alcance de Modificación

* **Nueva migración** `apps/backend/prisma/migrations/20260903100000_sync_schema_drift/migration.sql` (✅ creada):
  ```sql
  ALTER TABLE "User" ADD COLUMN "mustChangePin" BOOLEAN NOT NULL DEFAULT true;
  ALTER TABLE "SystemSettings" ADD COLUMN "idleTimeoutMinutes" INTEGER NOT NULL DEFAULT 15;
  ```
  Derivada de `prisma migrate diff --from-config-datasource prisma.config.ts --to-schema prisma/schema.prisma --script` contra una BD efímera con las 4 migraciones previas aplicadas. `User.email` ya estaba cubierto — no se toca.
* **Nuevo gate** `docs/04_governance_and_quality/scripts/check_migration_schema_parity.sh` (C-DEV-005-2, ✅ creado y verde): siempre-on; levanta Postgres efímero, aplica `migrate deploy` y falla si `migrate diff` contra `schema.prisma` no es vacío. Wireado en `docs/00_stack_manifest.md §7` y en `AGENTS.md` Guard 27 (g).
* **Decisión abierta (Guard 28) para el humano:** ¿`docker-entrypoint.sh` debe pasar a `prisma migrate deploy` (consistente con el comentario del `Dockerfile` y Guard 25) o se mantiene `db push` como mecanismo oficial y se ajusta el comentario? Este ticket arregla las migraciones para que ambos caminos converjan; la conmutación del entrypoint queda como sub-decisión.

**Fuera de alcance:** cambiar el mecanismo de despliegue del entrypoint (sub-decisión arriba); cualquier cambio de datos.

---

## ✅ Criterios de Aceptación & DoD

1. `bash docs/04_governance_and_quality/scripts/check_migration_schema_parity.sh` → verde (sin drift). ✅ verificado
2. `bash docs/04_governance_and_quality/scripts/check_seed_idempotency.sh` → verde (migrate deploy + seed x2 idempotente contra Postgres real efímero). ✅ verificado
3. El stack Docker (`docker compose up -d --build`) sigue arrancando y sembrando igual (sin regresión del camino `db push`).
4. `pnpm --filter @restostock/backend test`, `build` — 0 errores.
5. Commit atómico `fix(db): sync migrations directory with schema drift (mustChangePin, idleTimeoutMinutes) (TK-094)`.

---

## 🔗 Trazabilidad

* Cierra: `AUDIT-DEV-005` D-6.
* Relacionado: `TK-092` (lo destapó), `TK-070` (introdujo `mustChangePin` sin migración), `TK-051` (bootstrap/seed).
