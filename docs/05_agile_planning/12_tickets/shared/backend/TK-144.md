---
document: technical_ticket
id: TK-144
related_story: N/A (Técnico — Fidelidad del gate local)
points: 3
type: backend
status: done
inputs:
  - docs/04_governance_and_quality/scripts/ci_local.sh
  - .github/workflows/ci.yml
---

# 🎟️ TK-144: `ci_local.sh` no reproduce `ci.yml` — 3 divergencias reales

> [📖 Índice](../../indice_tickets.md)

## 📝 Descripción

`ci_local.sh` (`TK-063`) se creó para *"reproducir localmente los 3 jobs reales de `ci.yml`, en el mismo orden y con los mismos comandos"*. **No lo hace**, y eso se pagó en la primera corrida real de CI (2026-09-09, run #13): `ci_local.sh` daba **38 pasos verdes** y el pipeline real falló en **2 jobs de 3**.

| # | Divergencia | Consecuencia observada |
| :-- | :--- | :--- |
| 1 | **Semgrep ausente.** `ci.yml` Job 2 corre `semgrep scan --config=p/security-audit --error` (Guard 33, bloqueante). `ci_local.sh` no lo ejecuta nunca. | Job 2 rojo: **11 hallazgos** (1 fixture de test con JWT embebido + 10 de `header-redefinition` en `nginx.conf`, introducidos por `TK-141`). |
| 2 | **Comando distinto para el mismo paso.** `ci.yml` usaba `npx prisma validate`; `ci_local.sh` usa `pnpm --filter @restostock/backend exec prisma validate`. | Job 3 rojo: sin pin, `npx` resolvió **`prisma@8.0.0-rc.13`** (un *release candidate*) donde `validate` ya no existe → exit 2. **Violación del Guard 30** en el propio pipeline. |
| 3 | **Generación de SBOM ausente.** `ci.yml` Job 3 corre `cdxgen` y publica el artefacto (Guard 33). `ci_local.sh` no. | Sin fallo aún, pero es superficie no verificada antes del push. |

El patrón es exactamente el **Anti-Gate-Hueco** de `.agents/rules/00_output_reporting_standard.md`: un gate que sale verde sin analizar lo que su nombre promete. La ironía es que `ci_local.sh` **es** un gate de verificación y cayó en el antipatrón que el proyecto documenta.

## 🔀 Alcance propuesto

1. **Semgrep en `ci_local.sh`** con el mismo auto-bootstrap que ya usa para `tofu`/`oasdiff`/`gitleaks` (descarga a `.ci-local-tools/` si falta, `skip_step` si no hay red). Versión pinneada del manifiesto: `1.174.0`.
2. **SBOM (`cdxgen`)** como paso informativo del Job 3.
3. **Auditoría de paridad comando a comando** entre ambos ficheros, y decidir si conviene generar `ci_local.sh` desde `ci.yml` en vez de mantenerlos a mano (la causa raíz de la divergencia 2 es precisamente el mantenimiento manual duplicado).
4. **`profile_test_suite.sh` cuenta mal los fallos.** En la corrida #14 reportó `Tests pasados: 830 / Tests fallidos: 4` con la suite entera en verde (830 = 587 backend + 243 frontend, y el paso `Run Test Suite` salió con código 0). La causa es `FAILED=$(grep -c "×\|FAIL" …)`, que cuenta cualquier línea con `FAIL` o el carácter `×` — incluidas las de `stderr` de *mock fallback* que los tests emiten a propósito. Un contador que inventa fallos inexistentes en un paso informativo erosiona la confianza en el resto del reporte: debe derivar el conteo del resumen real del runner, no de un `grep` sobre todo el log.
5. Nota en la cabecera de `ci_local.sh` declarando qué pasos **no** reproduce y por qué, para que un verde local no vuelva a leerse como garantía total.

## ✅ Criterios de Aceptación & DoD

1. `ci_local.sh` ejecuta Semgrep con el mismo `--config` y flags que `ci.yml`.
2. Se demuestra la paridad: una divergencia introducida a propósito en `ci.yml` hace fallar también a `ci_local.sh`.
3. La cabecera del script lista explícitamente las exclusiones restantes.
4. **Commit:** `ci(quality): make ci_local reproduce the real pipeline (TK-144)`.

## 📊 Estado de Implementación (2026-09-09)

**Done.** Las tres divergencias cerradas y el contador falso corregido.

| # | Divergencia | Resolución |
| :-- | :--- | :--- |
| 1 | Semgrep ausente | Añadido como paso del Job 2, en el mismo orden que `ci.yml` y con los mismos flags (`--config=p/security-audit --error --metrics=off`). Auto-bootstrap propio: al ser un paquete Python y no un binario suelto, se instala en un **venv aislado** dentro de `.ci-local-tools/`, cacheado entre corridas, sin tocar el Python del sistema. `skip_step` si no hay red o `python3-venv`, igual que `tofu`/`oasdiff`/`gitleaks`. |
| 2 | Comando distinto en `prisma validate` | Ya corregido en el hotfix previo (`ci.yml` y `AGENTS.md` usan el binario pinneado del workspace). |
| 3 | SBOM ausente | Añadido al Job 3 tras el build, con `cdxgen` pinneado a la versión del manifiesto. |
| 4 | `profile_test_suite.sh` inventaba fallos | El conteo ya no sale de `grep -c "×\|FAIL"` sobre el log entero —que contaba las líneas de `stderr` que los tests emiten a propósito— sino de la **línea de resumen del runner**, la única con autoridad sobre el resultado. Si esa línea no aparece, lo dice en vez de reportar un número inventado. |
| 5 | Cabecera sin declarar exclusiones | Bloque nuevo *"QUÉ NO REPRODUCE"* al inicio del script. |

**Criterio de aceptación #2 — demostrado, no afirmado.** Se introdujo una divergencia deliberada (retirar la exclusión de `*.test.ts` del `.semgrepignore`, es decir, simular que `ci.yml` escanea algo que el gate local no cubría) y `ci_local.sh` **falló** (`exit 1`); al restaurarla volvió a `exit 0`. La paridad ya no es una promesa de la cabecera: es una propiedad comprobada.

**Lo que sigue sin reproducirse, ahora declarado en la cabecera** en vez de descubrirse por sorpresa: pasos de entorno del runner (checkout, `setup-node`, `setup-pnpm`, cachés — una diferencia de versión de Node **no** la detecta este script), la subida del artefacto SBOM, el mutation testing (salvo `--with-mutation`), y el servicio PostgreSQL efímero del Job 3.

## 📌 Notas

*   **Post-entrega.** Los fixes que ponen CI en verde (Prisma pinneado, `.semgrepignore` de fixtures, `nosemgrep` justificado en `nginx.conf`) se aplicaron ya en el commit correspondiente. Este ticket ataca **por qué no lo supimos antes**, que es lo que evita la próxima sorpresa.
*   El fix del Guard 30 sobre `npx prisma` también se aplicó a `AGENTS.md`, que documentaba el mismo comando roto en su lista de comandos canónicos.
