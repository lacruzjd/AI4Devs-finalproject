---
document: technical_ticket
id: TK-099
related_story: US-014 · AUDIT-DEV-006 F-3/F-4/F-7/F-8/F-9
points: 5
type: backend
status: done
inputs:
  - docs/audits/AUDIT-DEV-006-warehouse-extraction-quality-report.md
  - docs/05_agile_planning/11_user_stories/stock/US-014.md
  - docs/04_governance_and_quality/rules/backend_rules.md
---

# 🎟️ TK-099: Saneamiento de la Capa de Aplicación de Extracción — Reloj/ID Inyectados, Excepción de Dominio y Auditoría (Backend)

> **Navegación del Framework SDD:**
> [⬅️ Volver a US-014 (11_user_stories/stock/US-014.md)](../../../11_user_stories/stock/US-014.md) | [📖 Índice de Tickets](../../indice_tickets.md) | [Matriz de Trazabilidad ➡️](../../../13_matriz_trazabilidad.md)

---

## 📝 Descripción
Deuda de calidad acotada sobre `RecordExtractionUseCase` (hallazgos Media/Baja/Info de `AUDIT-DEV-006`). Sin cambio de comportamiento observable salvo el endurecimiento de auditoría (F-8) y la forma de `remanenteId` en descarte (F-9).

* **F-3 — IDs y reloj en la capa de aplicación:** `mov-${Date.now()}` **sin sufijo aleatorio** (colisión de PK en el mismo ms → `500`); `rem-${Date.now()}-${random(1000)}` no-UUID; `Date.now()` / `new Date()` en aplicación/dominio rompen la pureza hexagonal (AGENTS §4) y rozan el Guard 3.
* **F-4 — `throw new Error` crudo** para "motivo obligatorio en descarte directo" (`RecordExtractionUseCase.ts:111-113`): no mapea a RFC 7807; si el caso de uso se invoca fuera del controlador (Zod `.refine` bypass) devuelve `500`.
* **F-7 — El movimiento guarda el `name` del sector, no el `id`** (`fromLoc: sector.name`): renombrar un `StorageLocation` desincroniza el histórico.
* **F-8 — `operatorId` aceptado desde el body:** `userObj?.id || parsedBody.operatorId`; US-014 §Decisiones exige que la autoría venga solo del token.
* **F-9 — `remanenteId: ''` como centinela** en la respuesta de descarte directo.

*   **ID US Relacionada:** `US-014`
*   **Módulo / Vertical Slice:** `stock` (Bodega y Auditoría)
*   **Estimación (Story Points):** 5
*   **Prioridad MoSCoW:** Should Have
*   **Prioridad Backlog:** 🟢 P2 - Media
*   **Prerrequisitos:** `TK-098` (se codifica encima de la refactorización transaccional para evitar conflicto de merge)

---

## 🔀 Alcance de Modificación (Hexagonal Layers)

### Domain
*   Nuevos puertos en `domain/shared/` (o `domain/stock/`): `Clock { now(): Date }` e `IdGenerator { next(prefix?: string): string }` (implementación por defecto: `crypto.randomUUID()`).
*   `Remanente.createNew(...)` deja de llamar `new Date()`: recibe `now: Date` (y opcionalmente el `id` ya generado) como parámetro.
*   Nueva excepción de dominio `DiscardReasonRequiredException extends DomainException` (o la jerarquía existente) → mapeada a `400`/`422` RFC 7807 por el `errorHandler`.

### Application
*   `RecordExtractionUseCase` recibe `Clock` e `IdGenerator` por constructor. Todos los `Date.now()` / `new Date()` / plantillas de id se sustituyen.
*   `handleDirectDiscard`: `throw new Error(...)` → `throw new DiscardReasonRequiredException()`.
*   `ExtractionResponseDTO.remanenteId` pasa a `string | null` (`null` en `DIRECT_DISCARD`).
*   **F-7 — DIFERIDO (ver Addendum):** requiere columna nueva en `StockMovement` + migración Prisma con aprobación humana (AGENTS §3) + cambio del contrato del histórico. Desproporcionado para un TK-099 P2 sin decisión de diseño previa → se abre `TK-101` como follow-up. El movimiento sigue guardando `fromLoc = sector.name`.

### Infrastructure
*   `stock.controller.ts`: la autoría sale del token (`req.user?.id ?? parsedBody.operatorId`) — el `operatorId` del body solo se usa cuando NO hay usuario autenticado (tests `requireAuth:false`). `operatorId` se mantiene en el `recordExtractionSchema` únicamente por esos tests.
*   `stock.routes.ts`: wiring de `systemClock` (`infrastructure/shared/systemClock.ts`) y `cryptoIdGenerator` (`infrastructure/shared/cryptoIdGenerator.ts`, `randomUUID`) como singletons importados — no `new` en el route creator (Guard 18). Los tests inyectan fakes deterministas.
*   `openapi.yaml` / `07_api_specification.md`: `remanenteId` nullable en la respuesta de extracción.

---

## ✅ Criterios de Aceptación & DoD
1. **TDD:**
   *   `Remanente.test.ts`: `createNew` con `now` inyectado fija `expirationDate = now + 24h` exacta (sin depender del reloj de la máquina).
   *   `RecordExtraction.test.ts`: `IdGenerator` fake → ids deterministas; dos extracciones en el mismo tick **no** colisionan.
   *   Descarte sin `reason` invocando el caso de uso directamente → `DiscardReasonRequiredException` (no `Error` genérico); vía HTTP sigue siendo `400` RFC 7807 (regresión del test existente `:155-169`).
   *   `operatorId` del body **ignorado** cuando hay `req.user` (test con auth activa: body trae `operatorId:'spoof'`, el movimiento registra el id del token).
   *   `DIRECT_DISCARD` → `remanenteId === null` en el payload.
2. **Guard 3:** `grep` de `new Date(` / `Date.now(` en `application/stock/use-cases/RecordExtractionUseCase.ts` → 0. (`Remanente.createNew` conserva un default `new Date()` para compatibilidad con `vi.setSystemTime` de los 10 tests de dominio existentes; el caso de uso siempre pasa `clock.now()`.)
3. **RFC 7807** en la nueva excepción; gates de diff verdes.
4. **Contrato:** `openapi.yaml` — `remanenteId` nullable en la respuesta de extracción.
5. **Sin regresiones:** `pnpm test` / `build` / `lint` verdes. Mutation score ≥ 70 % por archivo (`RecordExtractionUseCase.ts`, `Remanente.ts`, `DiscardReasonRequiredException.ts`).
6. **Commit atómico:** `refactor(stock): inject clock/id generator, domain exception and audit hardening in warehouse extraction (TK-099)`.

## 🧪 Plan de Pruebas
- Unitario caso de uso (`RecordExtractionUseCase.test.ts`): ids deterministas vía `IdGenerator` fake (`rem-1`/`mov-1`, sin colisión entre llamadas), `expirationDate = clock.now() + 24h`, `DiscardReasonRequiredException`, `remanenteId` null.
- Integración HTTP (`RecordExtraction.test.ts`): descarte sin motivo → `400` `ValidationError` (Zod, regresión); las 12 pruebas existentes en verde.

## 📌 Addendum de Resolución (2026-09-04)

Implementado F-3, F-4, F-8, F-9. **F-7 diferido a `TK-101`** — necesita columna `StockMovement.fromStorageLocationId` + migración Prisma (aprobación humana, AGENTS §3) + cambio del contrato de `GET /stock/movements`; no cabe en un TK-099 P2 sin una decisión de diseño previa.

| Hallazgo | Estado | Resolución |
| :-- | :-- | :-- |
| **F-3** | ✅ | Puertos `domain/shared/Clock.ts` + `domain/shared/IdGenerator.ts`; impls singleton `infrastructure/shared/systemClock.ts` + `cryptoIdGenerator.ts` (`randomUUID`). `RecordExtractionUseCase` los recibe por constructor. Ids `mov-`/`rem-` ahora UUID (sin colisión de PK por ms). `Remanente.createNew(..., now)` inyectable. Cero `new Date()`/`Date.now()` en el caso de uso. |
| **F-4** | ✅ | `DiscardReasonRequiredException extends DomainError` (400). El `throw new Error(...)` crudo eliminado. La frontera Zod sigue devolviendo `ValidationError` primero por HTTP. |
| **F-8** | ✅ | `stock.controller.ts`: `req.user?.id ?? parsedBody.operatorId` (`??`, no `||`) + comentario explícito — con auth activa el body se ignora. |
| **F-9** | ✅ | `ExtractionResponseDTO.remanenteId: string \| null`; `null` en descarte. Tipo frontend `ExtractionResult` alineado; `buildLocalRemanenteFromExtraction` estrecha el tipo en el call-site guardado. |
| **F-7** | ⏸️ → `TK-101` | Sin cambio; `fromLoc = sector.name`. |

Deuda relacionada detectada (fuera de alcance): `RestockInsumoUseCase.ts:47` y `CreateLocationUseCase.ts:16` tienen el mismo patrón `` `xxx-${Date.now()}` `` — menor frecuencia (ops de admin), candidatas a `TK-101` o a un barrido `IdGenerator` posterior.
