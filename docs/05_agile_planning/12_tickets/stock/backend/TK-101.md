---
document: technical_ticket
id: TK-101
related_story: US-011 · US-014 · AUDIT-DEV-006 F-7
points: 3
type: backend
status: done
inputs:
  - docs/audits/AUDIT-DEV-006-warehouse-extraction-quality-report.md
  - docs/05_agile_planning/12_tickets/stock/backend/TK-099.md
  - docs/03_persistence_and_api/06_database_schema.md
---

# 🎟️ TK-101: Trazabilidad del Sub-Sector de Origen por ID en `StockMovement` (AUDIT-DEV-006 F-7)

> **Navegación del Framework SDD:**
> [⬅️ Volver a US-011 (11_user_stories/stock/US-011.md)](../../../11_user_stories/stock/US-011.md) | [📖 Índice de Tickets](../../indice_tickets.md) | [Matriz de Trazabilidad ➡️](../../../13_matriz_trazabilidad.md)

---

## 📝 Descripción
`AUDIT-DEV-006` F-7 (diferido de `TK-099` por requerir migración + decisión de diseño): el movimiento de extracción guarda `fromLoc = sector.name` (el **nombre** del sub-sector), no su id. Renombrar un `StorageLocation` desincroniza el histórico de movimientos y rompe cualquier filtrado por id.

Además: barrido del mismo patrón `` `xxx-${Date.now()}` `` (AUDIT-DEV-006 F-3, generalizado) en `RestockInsumoUseCase.ts:47` (`mov-`) y `CreateLocationUseCase.ts:16` (`loc-`) — mismo riesgo de colisión de PK, menor frecuencia. Se cierran aquí reutilizando el puerto `IdGenerator` ya introducido en `TK-099`.

*   **ID US Relacionada:** `US-011` (trazabilidad de movimientos) · `US-014`
*   **Módulo / Vertical Slice:** `stock`
*   **Estimación (Story Points):** 3
*   **Prioridad MoSCoW:** Should Have
*   **Prioridad Backlog:** 🟢 P2 - Media
*   **Prerrequisitos:** `TK-099`

---

## 🔀 Alcance de Modificación

### Persistencia (requiere aprobación humana del archivo de migración — AGENTS §3)
*   `schema.prisma` `StockMovement`: `+ fromStorageLocationId String? @db.Uuid` con relación opcional `storageLocation StorageLocation? @relation(..., onDelete: SetNull)` + `@@index`. Nullable — los movimientos históricos y los de cocina (`fromLoc = 'KITCHEN_*'`) no tienen sub-sector de bodega.
*   **Migración Prisma dedicada** (`prisma migrate dev --name stock_movement_from_sector`): sólo `ADD COLUMN` + FK + índice, sin backfill (los históricos quedan `NULL`).
*   `check_schema_drift.sh` / `check_migration_schema_parity.sh` / `check_seed_idempotency.sh` en verde tras la migración.

### Application / Infra
*   `StockMovementRecord` (`IRemanenteRepository`): `+ fromStorageLocationId?: string`.
*   `RecordExtractionUseCase` / `handleStockExtraction` / `handleDirectDiscard`: pasar `fromStorageLocationId` al `recordMovement` además del `fromLoc` (que se conserva como nombre para lectura directa).
*   `PrismaStockRepository.recordMovementOn` / `InMemoryStockRepository.recordMovement`: persistir el nuevo campo.
*   `GetStockMovementHistoryUseCase` / `PrismaStockMovementQueryRepository`: exponer `fromStorageLocationId` y resolver `fromLoc` por join al `StorageLocation` actual cuando exista el id (para que renombrar el sector se refleje en el histórico), con fallback al valor guardado.
*   `RestockInsumoUseCase` / `CreateLocationUseCase`: recibir `IdGenerator` por constructor; `` `mov-${Date.now()}` `` / `` `loc-${Date.now()}` `` → `idGenerator.next(...)`.
*   `openapi.yaml` / `07_api_specification.md`: `StockMovementHistoryItem + fromStorageLocationId`.

---

## ✅ Criterios de Aceptación & DoD
1. **TDD:** el movimiento de extracción persiste `fromStorageLocationId`; el histórico lo expone; renombrar el `StorageLocation` cambia el `fromLoc` mostrado en el histórico (join), no el guardado. `RestockInsumoUseCase`/`CreateLocationUseCase` con `IdGenerator` fake → ids deterministas.
2. **Migración verificada** contra BD efímera (`docker` + `migrate deploy` + parity + seed idempotency).
3. **Contrato:** `openapi.yaml` lint sin errores; `check_contract_drift.sh` reporta el campo nuevo como cambio documentado.
4. **Sin regresiones:** `pnpm test` / `build` / `lint` verdes; mutation ≥ 70 % por archivo tocado.
5. **Commit atómico:** `feat(stock): trace origin sub-sector by id on stock movements + IdGenerator sweep (TK-101)`.

## 📌 Notas de implementación
*   **`fromStorageLocationId` YA estaba disponible como variable local** en ambas ramas de `RecordExtractionUseCase` (`handleStockExtraction`/`handleDirectDiscard`) — el cambio fue puramente pasarlo a `recordMovement`, sin resolver nada nuevo.
*   **Join-preference implementado en ambos repos de consulta** (no solo Prisma): `PrismaStockMovementQueryRepository` (`include: fromStorageLocation`) y `InMemoryStockMovementQueryRepository` (nuevo `locationRepository?` opcional + `resolveCurrentSectorName`) — necesario para que el DoD #1 ("renombrar el `StorageLocation` cambia el `fromLoc` mostrado") sea verificable con un test rápido, no solo contra Postgres real.
*   **`app.ts` reordenado:** `buildAuxiliaryRepositories` (que construye `locationRepo`) ahora corre ANTES que `buildQueryRepositories`, para poder inyectar `locationRepo` en `InMemoryStockMovementQueryRepository`. Sin impacto en producción (`PrismaStockMovementQueryRepository` resuelve su propio join vía Prisma `include`).
*   **`RestockInsumoUseCase`/`CreateLocationUseCase` ganan `IdGenerator` como parámetro requerido** (no opcional con default) — TS forzó a actualizar los 2 call sites de producción (`stock.routes.ts`, `locations.controller.ts`) y los 2 de test que quedaban con la firma vieja, sin dejar ninguno sin migrar.
*   **Migración `20260904200000_stock_movement_from_sector`:** `ADD COLUMN` + índice + FK `ON DELETE SET NULL`, sin backfill (los movimientos históricos y los que nunca tuvieron sub-sector de origen — `RESTOCK`, `CONSUMPTION`, `SHIFT_RECONCILIATION_VARIANCE` — quedan `NULL`, por diseño). Verificada contra Postgres 15 real en una transacción con `ROLLBACK`; `check_migration_schema_parity.sh` (Postgres efímero) y `check_seed_idempotency.sh` (dos corridas reales) en verde.
*   **De paso, dos correcciones pequeñas de documentación** encontradas al tocar los mismos bloques: `openapi.yaml`'s `StockMovementHistoryItem.createdAt` tenía un `enum: [ACTIVE, CONSUMED, DISCARDED]` pegado por error de copia (un campo `date-time` con un enum de estados de remanente); y `07_api_specification.md` nunca había documentado `GET /api/v1/stock/movements` en absoluto, a pesar de existir desde `TK-050`.
*   **Cierra `AUDIT-DEV-006` por completo** — los 9 hallazgos (F-1 a F-9) quedan resueltos entre `TK-098`/`TK-099`/`TK-100-FE`/`TK-101`.
*   DoD #4 mutation ≥ 70 % por archivo: verificado (ver reporte del gate en el commit).
