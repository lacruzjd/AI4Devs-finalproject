---
document: maintenance_review
id: MNT-001
version: 1.0.0
status: closed
reviewed_on: 2026-09-14
---

# MNT-001: Primera revisión de mantenimiento de RestoStock

Primera ejecución real del workflow 11. Parte de cero: no había revisiones anteriores.

## Dependencias y vulnerabilidades
- `pnpm audit --audit-level=high` ejecutado contra el registro real el 2026-09-14: 2 avisos high/critical (`vite` `GHSA-fx2h-pf6j-xcff` y `vitest` `GHSA-5xrq-8626-4rwp`), ambos ya documentados como riesgo residual de solo desarrollo en `check_dependency_audit.sh`, que pasa en verde. Ningún aviso high/critical nuevo.
- 7 avisos moderados o bajos. `qs` 6.15.3 es el único alcanzable en producción: llega por `express@4.22.2` y `body-parser@1.20.6`. `esbuild`, `vite` y `@babel/core` solo intervienen en desarrollo, y `ajv` llega por herramientas de desarrollo (Prisma dev, Stryker) sin uso de la opción `$data` en el código.
- Al revisar el gate se detectó que pasa en verde si `pnpm audit` no llega a ejecutarse.
- **No ejecutado:** escaneo de la imagen de contenedor con `trivy`, por falta de espacio en disco en la máquina de la revisión. Queda pendiente para la próxima revisión.

## Deuda técnica
No existe índice de deuda técnica. La deuda conocida está repartida en 17 tickets con una sección "Deuda Registrada" y en más de 20 informes de `docs/audits/`. **No ejecutado:** el gate de código muerto, que está acotado a los cambios de un ticket y no ofrece una vista del repositorio completo.

## Feature flags pendientes de retirar
Ningún feature flag registrado: no hay registros de release con estrategia `flag` ni herramienta de flags declarada en el stack manifest.

## Operación
El despliegue en Render no tiene registro de release, SLOs ni backups declarados, y el gate `operacion` no puede verlo sin un registro de release. El stack manifest declara Render solo como entorno de revisión para el tutor, no como producción del restaurante.

## Especificaciones y ciclo
- `check_spec_artifacts.py` sobre el repositorio: 428 hallazgos de deuda documental (2 de KPIs en prosa, 158 de historias, 241 de Definition of Ready y 27 de trazabilidad). Sin KPIs con fecha de revisión vencida ni postmortems abiertos.
- Tickets aprobados pendientes: `TK-145` cumple la Definition of Ready; `TK-140` no la cumple.
- Candidatos a retirada: ninguno identificable, porque no hay datos de uso real con los que medir qué funcionalidades no se usan.

## Hallazgos
- `qs` 6.15.3 con dos avisos moderados, alcanzable en producción por `express` y `body-parser` — TK-146
- `check_dependency_audit.sh` informa 0 vulnerabilidades y pasa en verde si la auditoría no se ejecuta — TK-147
- Deuda técnica sin índice, repartida en tickets y auditorías — TK-148
- Avisos moderados y bajos solo de desarrollo (`esbuild`, `vite`, `@babel/core`) y `ajv` sin `$data` — sin acción — no son alcanzables en producción
- Render sin registro de release, SLOs ni backups — sin acción — es un entorno de revisión para el tutor, no producción; la operación y el release se diseñan antes del primer despliegue a producción real
- 428 hallazgos de deuda documental de especificaciones — sin acción — el humano decidió el 2026-09-13 no migrar la deuda documental por ahora
- `TK-140` aprobado sin cumplir la Definition of Ready — sin acción — `/momoy-dev` se detiene en la Definition of Ready cuando se retome, y la especificación se corrige entonces
