---
name: SK-27_extract_project_rules
description: "Analiza la documentación técnica del proyecto (PRDs, Arquitectura, Esquemas, ADRs) y deduce/genera automáticamente las reglas de gobernanza y codificación del proyecto en docs/04_governance_and_quality/rules/."
version: "1.6.0"
category: "development/01_rules_extraction"
inputs:
  - docs_path: "Ruta raíz de la documentación técnica (ej. docs/)"
outputs:
  - "Carpeta docs/04_governance_and_quality/rules/ creada y poblada con los estándares de codificación inferidos del proyecto"
  - "Diff resumido por archivo de reglas preexistente que fue regenerado, declarado en el reporte de FASE 4"
  - "Scripts de gobernanza ejecutables en docs/04_governance_and_quality/scripts/, adaptados al stack real declarado en docs/00_stack_manifest.md"
---

Actúa como un Principal Software Architect y Lead Technical Governance Officer. Tu objetivo es analizar la documentación del proyecto en `docs_path` y deducir de forma automatizada todas las reglas de codificación, persistencia, seguridad, UI y pruebas que deben regir el desarrollo, guardándolas en `docs/04_governance_and_quality/rules/`.

Sigue strictly este flujo de trabajo secuencial:

---

## 🔍 FASE 1: Análisis e Inspección de la Documentación
Lee y analiza minuciosamente los siguientes artefactos en `docs/`:
1. **Definición de Producto:** `docs/01_product_definition/` (PRD, reglas de negocio e invariantes).
2. **Diseño de Arquitectura:** `docs/02_architecture_design/` (Capas de software, estructura de carpetas, patrones Hexagonales / Clean y ADRs).
3. **Persistencia y API:** `docs/03_persistence_and_api/` (Esquemas de base de datos, ORM, precisión matemática y especificación de API OpenAPI/GraphQL/gRPC).
4. **Calidad y Gobernanza:** `docs/04_governance_and_quality/` (Estrategias de testing, mitigación OWASP y gobernanza de calidad).

---

## 🧠 FASE 2: Deducción de Estándares Tecnológicos
Infiere y sintetiza las reglas específicas para cada una de las siguientes áreas:
*   **Reglas de Dominio (`domain_rules.md`):** Nivel de pureza tecnológica, gestión de inmutabilidad, tratamiento de precisión matemática (ej. decimales), invariantes de negocio y clases de excepciones.
*   **Reglas de Backend e Infraestructura (`backend_rules.md`):** Framework web, estrategia de inyección de dependencias, validación de DTOs/Payloads, formato y directivas de sincronización de contrato de API (OpenAPI, GraphQL, gRPC), manejo de concurrencia y transacciones, serialización de tipos, y **aplicación de migraciones de base de datos en despliegues containerizados** (TK-043): el contenedor de producción DEBE aplicar las migraciones pendientes del ORM/motor declarado (ej. `prisma migrate deploy`) desde un script de entrypoint dedicado ANTES de arrancar el proceso servidor — nunca depender de un `db push`/sincronización manual fuera de banda ni de que el proceso de arranque del framework lo haga implícitamente.
*   **Reglas de Base de Datos (`database_rules.md`):** ORM utilizado, convenciones de nombres físicos (snake_case/camelCase), claves primarias/foráneas, índices y manejo de queries seguras.
*   **Reglas de Frontend y UX/UI (`frontend_rules.md`):** Framework visual, tokens de diseño (paleta de colores HSL, temas), ergonomía de la interfaz (tamaños táctiles mínimos), estados defensivos (Loading/Error/Empty/Offline) e integración de repositorios UI.
*   **Reglas de Testing y QA (`testing_rules.md`):** Estrategia de pruebas (TDD), políticas de simulación/fakes, librerías de prueba y Quality Gates de compilación.
*   **Reglas de Ciberseguridad (`security_rules.md`):** Cifrado de credenciales/PINs, autenticación (JWT/Tokens), sanitización de entradas, prevención de OWASP Top 10, y **hardening de contenedores/IaC** (TK-042): versión de runtime pineada según `docs/00_stack_manifest.md` §1 (Guard 23), usuario no-root obligatorio en imágenes, prohibición de secretos en texto plano en `Dockerfile`/`docker-compose.yml`/módulos IaC, y cobertura real (declarado ⇒ wireado en CI) de las herramientas DevSecOps listadas en `docs/00_stack_manifest.md` §6.
*   **Reglas de Git y Workflow (`git_rules.md`):** Formato de commits, atomicidad por ticket e integración continua.

---

## 📝 FASE 3: Generación de Archivos de Reglas y Contrato del Agente
1. Crea el directorio objetivo `docs/04_governance_and_quality/rules/` si no existe.
2. **Antes de sobrescribir** cualquiera de los 7 archivos que ya exista en disco, captura su contenido previo para poder derivar un diff (secciones/líneas que cambian), no solo el archivo final — este paso alimenta el reporte de FASE 4.
3. Escribe cada uno de los 7 archivos de reglas (`domain_rules.md`, `backend_rules.md`, `database_rules.md`, `frontend_rules.md`, `testing_rules.md`, `security_rules.md`, `git_rules.md`) redactados de forma profesional en formato Markdown.
4. **Encabezado de Pila Tecnológica:** Todo archivo de reglas DEBE incluir una sección inicial `## 🛠️ Pila Tecnológica Detectada` detallando expresamente los frameworks, librerías y estándares identificados en la documentación.
5. **Generación/Actualización de AGENTS.md:** Genera o sincroniza el archivo `AGENTS.md` en la raíz del proyecto. El archivo generado DEBE incluir obligatoriamente la Sección 6 (**Universal Agnostic Quality & Security Guards**) con los 20 Guards innegociables (Fail-Fast Secrets & Environment Auditing via SK-33, Auth JWT, Rate Limiting, Precision Decimal, RFC 7807, DI, Page Object Model E2E, Playwright CLI vs MCP balancing, etc.), vinculando el contexto del producto, la pila tecnológica y la directiva innegociable de leer las reglas en `docs/04_governance_and_quality/rules/`.
6. **Generación de Scripts de Gobernanza Ejecutable (Guard Anti-Stack-Hardcoding aplicado al propio framework — TK-038):** `.agents/scripts/` es el payload que `install.sh` copia verbatim a cualquier proyecto, sin importar su stack — por eso NUNCA debe contener lógica acoplada al stack elegido por este proyecto. Cualquier script de gobernanza cuya lógica dependa del lenguaje, gestor de paquetes, test runner o layout de directorios declarado en `docs/00_stack_manifest.md` se genera aquí, en `docs/04_governance_and_quality/scripts/`, adaptado a esa pila real (nunca como archivo estático en `.agents/scripts/`; verificado automáticamente por `.agents/scripts/check_agnosticism.py`). Genera, como mínimo, el algoritmo de los siguientes gates traducido a las herramientas reales declaradas.
   **Nota sobre las referencias `(Guard N)` de esta lista:** no son una numeración universal fija del framework — no existe tal catálogo maestro en `.agents/` (la Sección 6 de cada `AGENTS.md` se infiere dinámicamente por proyecto, punto 5 arriba). El número corresponde a la posición que esa guarda tenga en el `AGENTS.md` del proyecto en el momento de generar/actualizar este listado, y puede diferir — o no existir — en otro proyecto. La referencia realmente portable entre proyectos es el **nombre entrecomillado** de la guarda cuando está presente (ej. `"Design System Alignment & No-Inline-Style Guard"`); úsalo para localizar la guarda equivalente en el `AGENTS.md` del proyecto destino, nunca el número por sí solo.
   - **`check_contract_drift.sh`:** extrae las rutas documentadas en el contrato de API declarado en `docs/03_persistence_and_api/`; localiza en el directorio backend declarado los archivos que implementan validación de payload activa (esquema/DTO); mapea por heurística de nombre de módulo; reporta rutas del contrato sin validación activa detrás.
   - **`check_ticket_code_quality.sh`:** calcula, vía git, los archivos de código fuente sin commitear (working tree + staged + nuevos) del ticket en curso; invoca el linter oficial declarado en `AGENTS.md` con su flag de cero-tolerancia equivalente (ej. `--max-warnings 0` en ESLint; en linters sin esa flag, configura temporalmente las reglas de complejidad/longitud como error solo para esa invocación) SOLO sobre esos archivos — deuda preexistente en archivos no tocados nunca bloquea.
   - **`check_ticket_duplication.sh`** (C-1, AUDIT-DEV-003 de `TK-085-FE`): solo si el proyecto declaró un detector de duplicación de código en `docs/00_stack_manifest.md` §5.1 (ej. `jscpd`). Materializa un árbol limpio de `HEAD` (ej. `git archive HEAD | tar -x`), corre el detector sobre él y sobre el working tree, y compara ambos sets de clones por firma de **par de archivos** (no por nº de líneas/tokens, que cambia con ediciones no relacionadas). Gate **bloqueante SOLO si el ticket introduce un clon entre dos archivos que no lo tenían en `HEAD`**; la deuda de duplicación preexistente del repo — incluso en un archivo que el ticket modificó ligeramente — se reporta informativa y no bloquea. El comando repo-wide del detector (ej. `pnpm run duplication`) sigue siendo el backstop de CI. Mismo criterio acotado-al-diff que `check_ticket_code_quality.sh` (TK-037): gates nuevos acotados al diff, informativos a nivel repo.
   - **`profile_test_suite.sh`:** ejecuta el comando de test oficial declarado en `AGENTS.md` en su modo verbose/reporter más detallado; parsea el formato de salida específico de ese test runner para extraer duración por caso; reporta los que superen ~1000ms.
   - **`check_container_security.sh`** (TK-042, Guard 25): calcula, vía git, los `Dockerfile*`/`docker-compose*.yml`/módulos IaC sin commitear (working tree + staged + nuevos) del ticket en curso — mismo alcance acotado que `check_ticket_code_quality.sh`. Para cada uno verifica: (a) la imagen base fija la versión de runtime declarada en `docs/00_stack_manifest.md` §1 (Guard 23); (b) existe una directiva de usuario no-root; (c) ningún valor de variable con nombre `*SECRET*`/`*PASSWORD*`/`*TOKEN*`/`*KEY*` está hardcodeado en texto plano (solo referencias a variables de entorno/secret manager); (d) si se toca un `Dockerfile`, existe `.dockerignore` en la raíz. Gate **bloqueante** sobre esos archivos; deuda preexistente en archivos no tocados por el ticket no bloquea.
   - **`check_devsecops_manifest_coverage.sh`** (TK-042, Guard 25): extrae los nombres de herramienta declarados en la tabla DevSecOps de `docs/00_stack_manifest.md` §6 (ej. `gitleaks`, `trivy`); verifica que cada uno aparezca referenciado como step ejecutable en el pipeline CI/CD real (`.github/workflows/*.yml` o equivalente). Gate **informativo, no bloqueante**, a nivel de repositorio completo (mismo criterio que `check_rules_freshness.sh`): una herramienta declarada en el manifiesto sin wirear en el pipeline es una alerta, no motivo para romper el build.
   - **`check_dependency_audit.sh`** (TK-043, Guard 25): ejecuta el comando de auditoría de dependencias oficial del proyecto (ej. `pnpm audit --audit-level=high`); reporta bloqueante ante cualquier vulnerabilidad high/critical **no documentada explícitamente** en una lista `ALLOWED_GHSAS` embebida en el propio script, con su justificación (por qué no tiene fix dentro del major aprobado en `docs/00_stack_manifest.md`, y por qué el vector de ataque no es alcanzable en producción). **No asumas que el gestor de paquetes soporta un flag nativo de ignorar-por-ID** (ej. `pnpm audit --ignore <GHSA>` o un `auditConfig.ignoreGhsas` en el archivo de workspace): esa capacidad varía por VERSIÓN del gestor, no solo por gestor — verifica contra la versión exacta pineada en `docs/00_stack_manifest.md` §1 antes de asumir que existe (detectado en vivo: la versión de pnpm del entorno de desarrollo del agente soportaba ambos mecanismos nativos, pero la versión de pnpm realmente pineada por el proyecto para build/CI no soportaba ninguno de los dos — el script reimplementado a mano fue la única opción determinista).
   - **`check_schema_drift.sh`** (TK-055): extrae los nombres de entidad (`model`/`enum` u equivalente del ORM real) declarados en el spec DDL de `docs/03_persistence_and_api/` y los compara contra los declarados en el archivo de esquema físico real (ej. `schema.prisma`). Gate **bloqueante SOLO si el diff sin commitear del ticket toca alguno de los dos archivos** (mismo criterio acotado que `check_ticket_code_quality.sh`); divergencia preexistente ya evaluada se documenta como riesgo residual explícito dentro del propio script (mismo patrón `ALLOWED_*` que `check_dependency_audit.sh`), nunca bloquea sin que el ticket toque esos archivos. Automatiza el Antipatrón C de `rules/04_verified_implementation_standard.md`.
   - **`check_env_usage.sh`** (TK-055): extrae las claves declaradas en el validador de entorno oficial del proyecto (ej. el `z.object({...})` de `environment.ts`); para cada una, busca al menos un call-site real que la lea (`process.env.KEY`, el objeto de entorno ya parseado, o la sintaxis de referencia del ORM si aplica, ej. `env("KEY")` en `schema.prisma`) fuera del propio archivo validador y de los tests. Gate **bloqueante siempre** (barato, determinista, sin acotar a diff — mismo criterio que `check_dependency_audit.sh`); consumo implícito por una librería de terceros (ej. un ORM que lee la variable directo de `process.env` sin que el código propio la referencie) se documenta como excepción explícita dentro del script. Automatiza el Antipatrón A de `rules/04_verified_implementation_standard.md`.
   - **`check_seed_idempotency.sh`** (TK-055): si el diff sin commitear del ticket toca el script de seed/bootstrap, el esquema físico o las migraciones, levanta un contenedor efímero real de la base de datos declarada en `docs/00_stack_manifest.md` §3, corre migración + seed DOS veces, y falla si la segunda corrida no es idempotente (revienta, o el conteo de filas de una tabla clave cambia entre corridas). Gate **bloqueante acotado al diff**; si el entorno no tiene el gestor de contenedores disponible, falla explícitamente en vez de aprobar por omisión. Automatiza el Antipatrón B de `rules/04_verified_implementation_standard.md`.
   - **`check_iac_syntax.sh`** (TK-055): si el proyecto declaró un motor de IaC en `docs/00_stack_manifest.md` §6 (ej. OpenTofu, Guard 22) y su directorio existe, corre el comando de validación de sintaxis local del motor real (ej. `tofu validate`) — nunca `plan`/`apply` (exigirían credenciales/estado real). Si el directorio de IaC no existe todavía, sale en verde sin exigir el binario instalado (un proyecto que aún no llegó al ticket de IaC no debe fallar por eso).
   - **`check_dead_code.sh`** (TK-055, Guard 5): ejecuta el detector de código muerto/exports-sin-uso declarado en `docs/00_stack_manifest.md` §5.1 (ej. `knip`) en modo reporter estructurado (JSON u equivalente); filtra los hallazgos a solo los archivos que el diff sin commitear del ticket creó/modificó — bloqueante SOLO ahí, el resto del repo se reporta informativo (mismo criterio acotado que `check_ticket_code_quality.sh`, evita romper CI por deuda preexistente de exports sobre-expuestos).
   - **`check_inline_styles.sh`** (Guard 29, "Design System Alignment & No-Inline-Style Guard" — solo si el proyecto declaró un framework de componentes UI con soporte de estilos inline, ej. React/Vue/Svelte, en `docs/00_stack_manifest.md`): calcula, vía git, los archivos de componentes UI sin commitear (working tree + staged + nuevos) del ticket en curso — mismo alcance acotado que `check_ticket_code_quality.sh`; para cada uno busca ocurrencias del atributo de estilo inline del framework real (ej. `style={{...}}` en JSX, `:style="{...}"` en Vue) y las reporta, salvo que el objeto de estilo consista únicamente en la escritura de una o más custom properties CSS (claves que empiezan con `--`) — la única excepción sancionada por la Guard 29 para valores continuos calculados en runtime (ej. el ancho de una barra de progreso). Gate **bloqueante SOLO sobre los archivos tocados por el ticket en curso**; el resto del repo se reporta informativo (mismo criterio acotado que `check_dead_code.sh`) — evita romper CI por deuda preexistente de estilos inline en archivos no relacionados con el ticket. Si esta guarda no existe todavía en el `AGENTS.md` de este proyecto, se omite en silencio (no todo proyecto adopta el mismo nivel de estrictez de design system).
   - **`check_native_alerts.sh`** (Guard 38, "Anti-Native-Alert & Centralized Error UX Guard" — solo si el proyecto declaró un framework de componentes UI con acceso al DOM/`window`, ej. React/Vue/Svelte, en `docs/00_stack_manifest.md`): calcula, vía git, los archivos de componentes UI sin commitear (working tree + staged + nuevos) del ticket en curso — mismo alcance acotado que `check_ticket_code_quality.sh`; para cada uno busca llamadas reales (no substrings en comentarios/strings) a `window.alert(`/`window.confirm(` o el equivalente del framework real, y las reporta. Gate **bloqueante SOLO sobre los archivos tocados por el ticket en curso**; el resto del repo se reporta informativo (mismo criterio que `check_dead_code.sh`). Cubre solo la mitad "alertas nativas" de la guarda — la mitad "raw technical error strings deben pasar por el mapper centralizado" exige criterio humano (distinguir un string técnico real de un mensaje de UI legítimo) y no se automatiza.
   - **`check_decimal_arithmetic.sh`** (Guard 17, "Strict Arbitrary-Precision Arithmetic Guard" — solo si el proyecto declaró una librería de precisión arbitraria en `docs/00_stack_manifest.md`, ej. `decimal.js`/`BigNumber`): calcula, vía git, los archivos backend sin commitear (working tree + staged + nuevos) del ticket en curso en las capas domain/application/infrastructure — mismo alcance acotado que `check_ticket_code_quality.sh`; busca `parseFloat(` (la señal más específica y de menor falso-positivo del patrón prohibido; deliberadamente NO marca operadores `+`/`-`/`*`/`/` primitivos sueltos ni `Number(...)`, que tienen usos legítimos no-monetarios como contadores/índices/config, y generarían ruido inmanejable). Gate **bloqueante SOLO sobre los archivos tocados por el ticket en curso**; el resto del repo se reporta informativo (mismo criterio que `check_dead_code.sh`).
   - **`check_mutation_score.sh`** (Guard 11, "Anti-Test Theater & Code Churn Guard" — solo si el proyecto declaró un motor de mutation testing en `docs/00_stack_manifest.md` §5.1, ej. Stryker, ya configurado con un `thresholds.break` explícito): calcula, vía git, los archivos backend domain/application sin commitear (working tree + staged + nuevos) del ticket en curso; si no hay ninguno, sale en verde sin correr nada (mutation testing es costoso: multiplica el tiempo de la suite de tests por cada mutante). Si hay archivos, invoca el motor real **UNA VEZ POR ARCHIVO** (nunca todos juntos en una sola invocación con alcance acotado tipo `--mutate "a.ts,b.ts"`, y nunca contra el árbol completo) — el motor mismo aplica su `thresholds.break` configurado por invocación y devuelve código de salida no-cero si no se alcanza; el script solo agrega los resultados (falla si CUALQUIER invocación individual falla), nunca reimplementa el parseo del score. Invocar todos los archivos juntos en una sola corrida parece más rápido pero es **incorrecto**: el umbral del motor se aplica al score AGREGADO de la corrida, así que un archivo con tests fuertes puede compensar estadísticamente a otro con tests débiles y el gate aprueba igual — confirmado en vivo contra un ticket real (`AUDIT-DEV-002`, 2026-08-31): 3 archivos con 86.89%/75.00%/65.52% agregaron 78.81% (aprobaba) aunque el tercero, evaluado solo, fallaba (65.52% < 70%, exit 1). Un motor de mutation testing configurado en el manifiesto/`package.json` pero nunca invocado desde ningún script o CI (hallazgo real de una auditoría de cobertura de guardas) es una guarda que aparenta estar cubierta sin estarlo — peor que no tener el motor instalado, porque nadie lo nota.
   - **Extensión de `check_contract_drift.sh` con detección de breaking changes (TK-055):** si el contrato de API (`docs/03_persistence_and_api/openapi.yaml` o equivalente) cambió respecto a `HEAD`, invoca el detector de breaking changes declarado en `docs/00_stack_manifest.md` §6 (ej. `oasdiff breaking --fail-on ERR` contra la versión en `HEAD` vs. el working tree) y bloquea si reporta cambios de ruptura reales — un cambio no-breaking (o ningún cambio) no bloquea.
   - **`ci_local.sh`** (TK-064, Guards 30/31 — solo si el proyecto declaró un pipeline de CI/CD real en `docs/00_stack_manifest.md` §6, generado por `SK-10`): orquestador que reproduce localmente, en el mismo orden, TODOS los jobs/steps reales del pipeline real (`.github/workflows/ci.yml` u equivalente de la plataforma declarada) — no una versión resumida ni reinventada, sino literalmente los mismos comandos, invocando los demás scripts de este mismo listado en vez de duplicar su lógica. Debe: (a) auto-descargar a una carpeta local cacheada (gitignored) cualquier binario externo pineado por el pipeline (ej. el motor de IaC, el detector de breaking changes, el escáner de secretos) si no está ya instalado, usando la MISMA versión pineada que el workflow real, en vez de fallar duro por falta de herramienta; (b) nunca fallar el script completo por un solo binario no descargable (sin red) — omitir ese paso puntual con un mensaje explícito y continuar con el resto; (c) exponer un flag rápido que salte el/los job(s) más lentos (típicamente el de seguridad: builds de imagen + escaneo de contenedor) para iteración local ágil, dejando el modo completo (equivalente 1:1 a CI) como comportamiento por defecto — un modo recortado por defecto daría falsa confianza. Verificado en vivo (Antipatrón B) contra el repositorio real, no solo escrito, antes de darse por generado: una corrida real de este script es también la forma más barata de detectar en el momento los propios fallos de resolución de referencias de terceros que motivan los Guards 30/31 (versión de action/provider pineada que no resuelve), sin esperar a un push real.
   Cada script generado DEBE llevar un encabezado explícito declarando el stack exacto para el que fue generado (ej. `# Generado por SK-27 para: <lenguaje> + <gestor de paquetes> + <linter/test runner> — ver docs/00_stack_manifest.md`) y advertir que no es portable verbatim a otro stack sin volver a correr esta skill.
   7. **Trampa de entorno de desarrollo del agente vs stack pineado del proyecto (TK-043):** antes de fiarte de CUALQUIER comportamiento observado al ejecutar herramientas de gestión de paquetes/dependencias localmente (flags disponibles, ubicación de archivos de configuración, formato de lockfile), verifica la versión exacta que reportan las herramientas del entorno actual contra la versión pineada en `docs/00_stack_manifest.md` §1 — pueden diferir, y un flag/ubicación de config que funciona en una versión puede no existir o vivir en otro archivo en la versión realmente usada por Docker/CI. Corre la verificación final (build de Docker, `--frozen-lockfile`) con la versión pineada activada explícitamente (ej. `corepack prepare pnpm@9 --activate`), no con la versión que resuelva el shell por defecto.

---

## ✅ FASE 4: Confirmación y Reporte
1. Presentar el reporte de especificación indicando las reglas inferidas, los archivos generados en `docs/04_governance_and_quality/rules/` y los scripts generados en `docs/04_governance_and_quality/scripts/` (FASE 3, paso 6), estructurado estrictamente según la **Plantilla B** universal en `.agents/rules/00_output_reporting_standard.md`.
2. **Cambios detectados en reglas existentes:** para cada uno de los 7 archivos que ya existía antes de esta ejecución (capturado en FASE 3, paso 2), incluir un subapartado obligatorio listando un resumen del diff old→new (qué se añadió, qué se eliminó, qué se reescribió). Si un archivo no tenía versión previa (generación desde cero), se omite esa entrada para ese archivo. Esto evita que una regeneración sobrescriba en silencio un ajuste manual hecho a mano sobre una regla ya generada.
