# Dependencies
node_modules/
jspm_packages/
web_modules/
.pnpm-store/

# Build and Compilation Output
dist/
build/
*.tsbuildinfo
# Cliente Prisma generado (TK-062, generator "prisma-client") — codigo generado, no se commitea
apps/backend/src/generated/

# Binarios auto-descargados por ci_local.sh (TK-063) — cache local, no se commitea
.ci-local-tools/

# SBOM generado por `cdxgen` en el Job 3 (ci.yml lo publica como artefacto de CI;
# ci_local.sh lo genera en la raíz desde TK-144). Es un artefacto de build, no fuente.
sbom.json
.next/
out/
.nuxt/
.cache/
.serverless/

# Environment Variables & Secret Credentials (Security Strategy)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.env.*
!.env.example

# Debug Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test Coverage (QA Strategy)
coverage/
.nyc_output/

# Mutation Testing Reports (Stryker)
**/reports/mutation/
.stryker-tmp/

# IDEs, Editors & Workspaces
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating System Files
.DS_Store
Thumbs.db
desktop.ini

# Database & Temporary SQLite Files
*.db
*.db-journal
*.sqlite
*.sqlite-journal

# Local Temp Folders
tmp/
temp/
.temp/
.tmp/
__pycache__/

# OpenTofu / Terraform State (puede contener secretos resueltos en texto plano — Guard 22/23)
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Playwright (e2e) run artifacts
apps/frontend/test-results/
apps/frontend/playwright-report/
apps/frontend/e2e-artifacts/
